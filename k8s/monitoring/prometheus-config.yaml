apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: monitoring
data:
  prometheus.yml: |
    global:
      scrape_interval: 5s

    scrape_configs:
      - job_name: 'flask-api'
        static_configs:
          - targets: ['flask-api-service.default.svc.cluster.local:80']